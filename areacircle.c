#include<stdio.h>

int main()
{
    int radius =7;
    float pi = 3.14;
    printf("the area of circle is %f", radius*pi*radius);
    int height =9;
    printf("the vo;oume of cylinder is %f ",  radius*pi*radius*height );
    return 0;
}
